// ✅  Dynamic Typing
//       JavaScript allows variables to change types on the fly.

let message = "Hello";  // message is a string
console.log(typeof message);  // "string"

message = 12345;  // now it's a number
console.log(typeof message);  // "number"
