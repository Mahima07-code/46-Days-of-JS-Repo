// Execution Stage – The Engine Runs the Code

function sayHello(name) {
  return "Hello, " + name + "!";
}

let message = sayHello("Mahima");
console.log(message);

// What Happens:
// The engine parses → no errors ✅

// It compiles sayHello() into machine code.

// Then it executes: